// alert ("Thanks for your input");  // alert practice ,, is this some message to the user

 // this type data is string data and is always writes in double , single and ` qoutation 

 var userName = "Umair Jamaat Ali"; 
 console.log ("userName" , userName)
 // this type data is call number data and its written always in numbers
 
 
 var cellNum = 03450712423 
 console.log ("Cell #", cellNum)
 
 // this type of data is floating data and its written in dicimal digits
 var dicimal = 654.987; 
 console.log ("decimal", dicimal)
 
 ///Math expresitions and operation
 var total = 2 + 4 - 8 * 8 ;
 console.log ("Is equal to", total)
 //sum (+)
 var sum = 2+7;
 console.log ("answer is", sum)
 // subracts (-)
 var sub = 99 - 56;
 console.log ("answer is", sub)
 
 // Multiple (*)
 var multi = 9 * 8;
 console.log ("answer is", multi)
 
 // Division (/)
 var divi = 90 / 11;
 console.log ("answer is", divi)
 
 
 
  // Solve a problems
  // Write a program that convert kilograms in grams
  var kg = prompt("Pleas enter the weight in kg")
  // (kg * 1000 grams) 
  var grams = (kg * 1000 );
 
  console.log("Weight in grams", grams)
 
  
  // Solve a problems
  // Write a program that convert mints into seconds
  var mints = prompt("Pleas enter the mints")
  // (mints * 60 seconds) 
  var seconds = (mints * 60 );
 
  console.log("Time in seconds", seconds)


  // solve a problem
  // Write a programs that convert single values into squares

  var singleNumber = prompt("Pleas enter the single value")
  // ( number * number )
  var number = 5;
  var squares = number * number ;
  
  console.log("Answer in squares", squares);


  // Generate a random number between 1 and 10
  var singleNumber = prompt("Pleas enter the single value")
var number = Math.floor(Math.random() * 10) + 1;

// Calculate the square of the number using Math.pow() method
var square = Math.pow(number, 2);

// Display the result
console.log("The square of " + number + " is " + square);


// Generate a random number between 1 and 10
  var singleNumber = prompt("Pleas enter the single value")
var number = Math.floor(Math.random() * 10) + 1;

// Calculate the square of the number using Math.pow() method
var square = Math.pow(number, 2);

// Display the result
console.log("The square of " + number + " is " + square);